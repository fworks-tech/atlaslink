// Shared constants mirrored from src/components/NewTaskForm (canonical source until removed).
// Order mirrors the reference "fix issue X" flow (the mediator routes first).
export const QUICK_MEMBERS = [
  "the-mediator",
  "the-debugger",
  "the-builder",
  "the-tester",
  "the-reviewer",
  "the-librarian",
] as const;
